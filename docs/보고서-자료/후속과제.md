# 미해결 · 미검증 · 후속 과제 (**2026-08-18 갱신** · 최초 2026-08-14)

> 보고서 "미해결 이슈 · 후속 과제" 칸의 원본. **완료로 부풀리지 않는 것이 이 문서의 목적**이다.
> 분류: 🔴 검증 필요(했다고 말하면 안 되는 것) / 🟡 남은 작업 / ⚪ 범위 외·보류(사용자 결정)

## 🔴 검증이 남은 것 — 보고서에 "완료"로 쓰면 안 된다

| 항목 | 지금 상태 | 무엇이 있어야 해소되나 |
|---|---|---|
| **실기 하드웨어** | SDK·launch·어댑터·안전감시 준비 완료, mock 배선 검증 통과 | 제어박스 연결. ⚠ `cb_safety_publisher` 의 **데이터 채널 두 번째 연결**이 미검증이라 기본 꺼짐 |
| **RG2 그리퍼 명령** | `gripper_macro` 인자 배치 확인 불가(레인보우 문서에 OnRobot 없음) | 매뉴얼/티치펜던트에서 func 확인 → 파라미터만 교체(코드 변경 0) |
| **재계획 트리거 ① 콜사이트** | ④ 구간에서만 발화 확인(08-16 에 앞당겨 시도했으나 또 ④ 였다) | 주입기가 움직임 감지 후 실제 주입까지 23초 걸린다 — 그 지연을 줄여야 ① 구간에서 발화한다 |
| **접촉·충돌 응답** | 🟡 **부분 해소(2026-08-18)** — 중력 처짐은 Unity 에서 실측했다(8/8 · 근거대장 6-6). 남은 것은 **접촉**이다: Gazebo 는 로봇 collision 을 제거했고, Unity 물리 시험도 콜라이더를 끄고 잰다 | 콜라이더를 켠 채로도 재려면 자기충돌 밀림과 중력 처짐을 **분리할 방법**이 먼저 필요하다(섞이면 원인을 못 가른다) |
| **드라이브 게인** | stiffness 1e4~1e7 / damping = **플레이스홀더**. 최악 자세 잔여 처짐 78.9 mm 가 여기서 나온다 | 실기 제어기 특성에 맞춘 게인 산정. ⚠ 이 값은 **실기 정확도 예측이 아니다** |
| 🆕 **절단 툴**(2026-08-20) | 절단 모드는 구현·검증됐으나 **툴이 정해지지 않았다** — 기존 RG2 로 "닫으면 잘린다"고 **가정**했다. 절단력·날 형상·잘린 화방이 손에 남는지(집게형)/떨어지는지(절단형) 미정 ⇒ 잘린 화방을 planning scene 에 **부착하지 않는다** | 툴 선정. 정해지면 채울 곳 = 날 위치(`cut_depth`)·닫힘축 유도·잘린 화방 부착 형상. **코드 구조는 그대로**(닫힘축은 URDF 에서 유도) |
| 🆕 **절단 대상이 인도 구성에 없다**(2026-08-20) | 화방대 21개 중 **0/21** — 도달거리가 아니라 **거터**(화방이 거터 상면 위 8.2 cm 뿐) | ① 작물 배치를 현실값(`first_z=0.35`)으로 되돌리고 ② **팔 베이스 +500 mm** 를 채택해야 5/21 성립. ⇒ 아래 '결정됨 · 스탠드' 와 **묶인 사안** |
| 🆕 **소과경(열매 개별 자루)**(2026-08-20) | 작물 모델에 없다 — 줄기 6마디 + **화방대 1개** + 열매 구. 자르는 대상은 화방 전체를 매다는 화방대다 | 열매별 자루를 모델에 추가할지 결정(`expand_crops` 확장). 현장 절단 위치가 소과경이라면 필요 |
| 🆕 **절단 모드 인지 미지원**(2026-08-20) | `fruit_detector` 는 빨강 세그로 열매만 찾고 줄기를 내지 않는다 ⇒ **설계값(yaml) 장면 전용** | 줄기·화방대 인지(색·형상 기반) 추가 |

## 🟡 남은 작업

> ✅ 2026-08-16~18 에 해소된 항목은 아래 "해소됨" 표로 내렸다 — **남은 것만 번호로 둔다.**

1. **접근 축 캡슐/원뿔** — 현재 구(ρ=15.7cm) 영역은 이웃 화방까지 무시하게 된다.
   근거 2건 확보됨. `publish_targets:=true` 조건에서 재측정 필요.
2. **RealSense TF 경계 결정** — URDF 는 마운트까지, 내부는 드라이버 TF. 안 정하면 **이중 발행**.
3. **`mimic` 대응** — USD·Unity URDF Importer 양쪽 다 mimic 이 없어 손가락 관절이 독립.
   컨트롤러가 같은 값을 주거나(Unity 쪽에서 가능) 시뮬레이터의 mimic/coupled joint 로 묶어야 한다.
   ※ Unity 는 실행 검증됐으므로 여기서 확인 가능. Isaac 쪽은 **범위 밖**이라 미해결로 남긴다.

### ✅ 해소됨 (기록 보존 — 보고서의 "무엇을 언제 끝냈나" 근거)

| 항목 | 해소 | 근거 |
|---|---|---|
| 3종 비교표(M4 마감 조건) | ✅ 2026-08-16 | `3종-비교표.md` · 근거대장 6-5 |
| 데모 캡처 | ✅ 2026-08-16 — 총 **15장**(재계획 발화·수확 전/후 비교컷 포함) | 근거대장 6-4 · 6-4-2 · 6-4-3 |
| Unity 설치·실행 검증 | ✅ 2026-08-16 — **20/20**(최초 17/17 + 온실 형상 검사 3종) | 근거대장 6-3 |
| Unity ↔ ROS 연동·수확 반영 | ✅ 2026-08-16 — **7/7** | 근거대장 6-3-2 |
| Gazebo 산출물 정리(M4 마감 항목) | ✅ **2026-08-18** — 인도 번들 `export/gazebo/` 152 KB 신설(3종이 같은 형식이 됐다) | 근거대장 6-1 |
| **유도 effort 값의 실전 시험**(중력 처짐) | ✅ **2026-08-18** — Unity 물리 시험 **8/8**. 유도값은 버티고(최악 자세 78.9 mm) **종전 플레이스홀더 10 Nm 는 무너진다**(825 mm) | 근거대장 6-6 |

## ✅ 결정됨 (2026-08-18)

### 팔 베이스 +500mm 스탠드 — **채택하지 않는다.** 검증된 개선안으로 인도한다.

| | 내용 |
|---|---|
| **결정** | **기본 구성 유지**(`mounts.yaml` 불변). 스탠드는 `mounts_stand.yaml` + 별도 모델로 **재현 가능한 시험 구성**으로만 남긴다 |
| **근거 ①(결정적)** | **실기가 스탠드를 갖고 있지 않다.** 2026-07-30 사용자 확인 = *"실측값은 지금 모델 구성된 위치와 동일"*. 스탠드를 기본으로 넣으면 **인도 모델이 실측된 실기와 어긋난다** — 이 프로젝트가 지켜 온 '모델=실기' 대응이 깨진다 |
| **근거 ②** | **스탠드는 구조 설계가 아니다** — Ø0.24×0.5 m 균질 원통 12 kg 개략 모델(좌굴·플랜지 체결·강성·동적 전도 전부 미반영). 설계되지 않은 부품을 인도 모델에 넣는 것은 *"미검증을 완료로 쓰지 않는다"* 원칙에 정면으로 어긋난다 |
| **근거 ③** | **전도각 30.8° → 22.7°**(약 8° 손해)를 운영 조건에 반영할 근거가 아직 없다(동적 전도 미계산) |
| **근거 ④(일정)** | 채택하면 `gen_srdf.py` 재실행 + **3종 산출물·검증 수치 전량 재생성**이 따라온다 — 조립 30/30 · USD 23/23 · Unity 20/20 · 연동 7/7 · 물리 8/8 · Gazebo 실증 · 근거대장/비교표/보고서 원고의 **모든 수치**. 남은 근무일 9일의 보고서 국면에서 감당할 범위가 아니고, 실익(실기에 없는 형상)도 없다 |
| **버리지 않는 것** | 시험 결과는 **그대로 보고서에 싣는다**(아래 🔴) |

🔴 **이 결정이 감추면 안 되는 사실 — 지금 작물이 로봇에 맞춰 낮춰져 있다.**
`obstacles.yaml` 의 `truss.first_z` 는 현실값 **0.35 → 0.10** 으로 내려가 있다. 스탠드 시험이
그 대가를 수치로 만들었다: **현실 배치에서 현재 구성의 도달 열매는 0/74**, 스탠드를 넣으면 16~17/74.
⇒ 보고서에 **"현 구성은 낮춘 작물 배치에서만 수확이 성립한다"** 를 명시하고,
   **"팔 베이스를 500mm 올리면 현실 배치에서 성립함을 시험으로 확인했다(채택은 미결정 아님 — 이번
   범위에서 채택하지 않기로 결정)"** 를 후속 권고로 함께 적는다.

**이어받는 쪽에 넘길 것**: `docs/도달권-스탠드-시험.md`(측정·조건·한계) ·
`docs/보고서-자료/스탠드-비교표.md`(있음/없음 비교) · `config/mounts_stand.yaml` ·
`config/models/base/scout_v2_stand50.{xacro,yaml}` · 재현 명령 일체.
**실제 도입하려면 먼저 필요한 것** = 라이저 구조 설계 · 동적 전도 해석 · Scout 2.0 정격 적재 확인.

## ⚪ 범위 외 · 보류 (사용자 결정)

| 항목 | 결정 | 시점 |
|---|---|---|
| mount 실측 정렬 | **생략** — "실측값이 지금 모델 구성 위치와 동일" | 2026-07-15 / 07-30 재확인 |
| base 전진·UR10e·레일 재배치 | **보류** — 레일 이탈·거터 침범 | 2026-07-22 |
| 파지 열매 물리 강체화 | **보류** — Gazebo Classic 파지 불안정, 7주차 Isaac 쪽 | 2026-07-29 |
| RG2 명령 확정 | **스킵** — 그리퍼가 바뀔 수 있음 | 2026-07-30 |
| Isaac Sim 설치 | **안 함** — 파일만 제공 | 2026-08-14 |
| **Isaac Sim 검증 전체** | 🔴 **이번 프로젝트에서 진행하지 않는다**(사용자 결정) — 실행·물리(팔 처짐)·드라이브 게인 조정 모두 **범위 밖**. 산출물(`export/isaac/`, 16MB)과 로컬 수치 검증 23/23 이 인도물이다. | **2026-08-16** |
| Unity 설치·실행 검증 | ✅ **완료** — 17/17 (`78c1bc3`·`d28b698`) | 2026-08-16 |
| **작물 모델 — 잎** | **넣지 않는다**(사용자 지시). 한 번 구현·표시까지 했다가 제거. ⚠ 그 결과 **통로 쪽 캐노피 깊이가 없다** — 회피 비교의 '정면 직선 진입 충돌 0'(`구성프리셋-선정규칙.md` 4장)이 성립하는 이유다. 이 한계는 **해소되지 않았다** | **2026-08-21** |
| **작물 모델 — 꽃받침(calyx)** | **넣지 않는다.** 의견이 아니라 **문턱값으로 확인**: 파지 자세에서 열매 꼭지↔그리퍼 여유가 탐색 격자 최악 **2.0 cm**(채택 각도 기준 최악 2.42 cm)이고, Ø70 mm 열매의 꽃받침(꼭지에서 1.5~2.5 cm)은 그 언저리~아래다. 재현 = `docs/scripts/calyx_clearance.py` · 상세 = `작물모델-파라미터.md` 5장. ⚠ **실기 파지 안정성·인지 편향은 이 판정으로 답하지 못한다**(모델에 접촉 물리가 없다) | **2026-08-21** |

> 🔴 **"범위 밖" 과 "미검증 부채" 는 다르다.** Isaac 실행 검증은 *못 한 것*이 아니라 *하지 않기로
> 결정한 것*이다. 보고서에는 **"실행 미검증(프로젝트 범위 밖)"** 으로 적고, 후속 조직이 이어받을 수
> 있도록 아래를 함께 남긴다 — 인도물 `~/robot_ws/export/isaac/`(README 포함) · 재현 명령
> `ros2 run rda_robot_bringup gen_usd_scene.py --out <dir>` · 검증 `python3 docs/scripts/test_usd_export.py`.
> **이어받는 쪽이 확인할 것** = 아티큘레이션 인식 · **중력에서 팔 처짐**(effort 유도값의 첫 실전 시험) ·
> 드라이브 게인(stiffness 1e4 / damping 1e3 = 플레이스홀더) · convexHull 충돌 근사 · `mimic` 부재.

## 참고 — 이 프로젝트에서 반복된 실패 유형(보고서 '교훈' 칸 후보)

1. **조용한 무효** — 에러 없이 아무 효과가 없던 조작이 4건 이상(ACM components 오지정, `is_diff`
   누락, 객체 추가 후 ACM 재적용 누락, 응답 success 신뢰). *교훈: 적용했으면 되돌려 읽어 확인한다.*
2. **원인 추정과 실측의 불일치** — 접촉 10건을 '구 영역' 탓으로 추정했으나 실제는 'Cartesian 해의
   손목 뒤집힘'. *교훈: 개수만 세지 말고 접촉 쌍·사유를 남긴다.*
3. **기록해 둔 교훈을 다시 밟음** — 하루에 3건(chmod +x, 남은 gzserver, 응답 success 신뢰).
   *교훈: 새 코드를 쓰기 전에 함정 목록을 먼저 훑는다.*
4. **측정자가 틀린 경우** — USD 검증 첫 실패 3건 중 2건이 검증 스크립트의 결함(scale 이 섞인 변환
   비교, float32 정밀도에 1e-9 요구). *교훈: 불일치를 보면 측정 도구부터 의심한다.*
