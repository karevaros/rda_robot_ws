# 보고서 문단 초안 — 관리 세션이 `연수결과-보고서.html` 로 옮길 원고

> 규칙: `작성지침.md`. **수치는 `근거대장.md` 에 있는 것만** 쓴다. ⚠ 표시는 옮길 때도 지운다.
> 지금 비어 있는 `[작성]` 칸 = 7주차 카드(수행 내용·산출물) · 마무리 카드 · 일정표 2행.

---

## 7주차 · 가상환경 컨버팅 (M4) — **진행 중**

### 일정표 행
| 주차 | 기간 | 내용 | 상태 | 비고 |
|---|---|---|---|---|
| 7주 | 8/24~8/28 | 가상환경 컨버팅 3종 검증 **(M4)** | **진행 중(선행 착수 2026-08-14)** | Gazebo 완료 · Isaac 파일 산출·로컬 검증 · Unity 프로젝트 산출·실행 대기 |

### 수행 내용 (초안)

**방향 결정(2026-08-14, 사용자 판단): 3종을 모두 이 PC 에 설치하지 않는다.**
개발 노트북은 여유 디스크 33GB · VRAM 8GB 로 Isaac Sim(20~30GB)과 Unity(10GB+)를 동시에 감당하기
어렵다. 그래서 **Isaac Sim 은 설치하지 않고 "Isaac 이 그대로 열 수 있는 파일"만 산출**하고,
**Unity 만 실제로 설치해 확인**하는 방식으로 전환했다. 이 전환으로 7주차의 두 관문(디스크·VRAM)이
사라진 대신, *설치 없이 만든 파일이 정말 맞는지를 어떻게 보증하나* 라는 과제가 새로 생겼다 —
이번 주차의 기술적 초점은 거기에 있다.

**① Gazebo(SDF) — 완료(선행).** 온실 월드 static 186개, D435i/D405 depth → MoveIt 옥토맵,
열매 인지, ros2_control 실구동 execute 까지 6주차에서 이미 라이브 검증됐다.

**② Isaac Sim(USD) — 파일 산출 + 로컬 수치 검증.**
Isaac 런타임 없이 **Pixar USD(`usd-core`)** 만으로 씬을 직접 작성했다. `usd-core` 에 `UsdPhysics`
스키마가 포함돼 아티큘레이션·질량·관성·관절 한계까지 표준 형식으로 기술할 수 있다는 점을 실측으로
확인한 뒤 진행했다. 신규 생성기 `gen_usd_scene.py` 는 **통합 URDF + `obstacles.yaml`**(Gazebo 월드와
같은 소스)에서 `rda_robot.usd`·`greenhouse.usd`·`rda_scene.usd` 를 만든다 — 세 시뮬레이터가 같은
단일 진실원을 쓰므로 형상·좌표가 자동으로 정합된다.
핵심 설계는 **fixed 관절 병합**이다. USD/PhysX 는 강체 안의 강체를 허용하지 않는데 URDF 44링크 중
질량이 있는 것은 16개뿐이라, fixed 로 이어진 링크를 강체 1개로 합치고(관성은 평행축 정리로 합성)
합쳐진 링크는 빈 프레임으로 남겨 **좌표 44개를 모두 보존**했다(강체 9개 = 1 + 가동관절 8).
검증은 Isaac 을 켜지 않고 **수치 대조 23/23** 으로 했다: 링크 프레임 44개의 world 변환이 URDF 정기구학과
오차 7.24e-08 m 이내로 일치, 관절 프레임이 부모·자식 양쪽에서 6.85e-08 이내로 만남, 관절 한계(도)가
URDF(라디안)와 일치, 질량 합 73.688 kg 일치, 온실 186개가 Gazebo SDF 와 자세·치수 일치,
합성 스테이지의 tcp 위치 `[0.2074, 0, 1.2119]` 가 실기 배선 검증값과 일치. 산출물은 16 MB.

**③ Unity — 프로젝트 산출 + 로컬 검증(실행 대기).**
`gen_unity_assets.py` 가 **열기만 하면 되는 Unity 프로젝트**를 만든다 — URDF 와 mesh 23개를
URDF Importer 가 `package://` 를 풀 수 있는 배치로 넣고, 온실 186개는 JSON + 에디터 스크립트로
정적 오브젝트로 세우며(186개를 관절 물체로 만들 이유가 없다), URDF Importer v0.5.2 의존성과
Unity 2022.3.62f3 버전을 프로젝트에 박아 둔다.
좌표계는 짐작하지 않고 Unity Robotics 원본 소스에서 확인해 옮겼고(위치 (x,y,z)→(−y,z,x),
회전 (x,y,z,w)→(−y,z,x,−w)), **그 변환식 자체를 무작위 2000회 수치 검증**했다(정합 오차 4.4e-16).
Unity 없이 확인 가능한 항목 **14/14** 통과.

**⚠ 미검증(정직 표기).** ⓐ **Isaac Sim 에서 실제로 열어보지는 못했다** — 검증한 것은 "USD 가 URDF 와
같은 로봇·같은 장면인가"이지 "Isaac 에서 잘 도는가"가 아니다. Isaac 쪽에서 아티큘레이션 인식,
중력에서의 팔 처짐(유도 effort 의 첫 실전 시험), 드라이브 게인(현재 플레이스홀더), 충돌 근사를
확인해야 한다. ⓑ **Unity 는 설치 보류 상태**(2026-08-14 사용자 결정, 자택 설치 예정)라 배치모드
실행 결과가 아직 없다. 실행하면 검증 스크립트가 프레임 위치까지 자동 대조한다.
ⓒ `mimic`(그리퍼 손가락)은 USD 표준에도 URDF Importer 에도 없어 양쪽 모두 관절이 독립이다.

### 산출물 (초안)
`rda_robot_bringup/scripts/gen_usd_scene.py`(USD 생성기) · `gen_unity_assets.py`(Unity 프로젝트
생성기) · `docs/scripts/test_usd_export.py`(23/23) · `test_unity_export.py`(14/14) ·
산출 번들 `export/isaac/`(rda_scene.usd 외, 16MB · Isaac 임포트 절차·한계 README 포함) ·
`export/unity/`(Unity 프로젝트 일체). 커밋 `00434b4`·`ea20a69`·`7ff85c0`(push 완료).

---

## 마무리 · 결과 정리 (8/31) — **미착수**

> 아래는 골격만. 채우는 조건을 함께 적어 둔다(조건이 안 채워지면 그 항목은 '미검증'으로 남긴다).

### 일정표 행
| 마무리 | 8/31 | 결과 정리 · 보고 | (진행에 따라) | — |

### 채울 항목
1. **3종 비교표** — 같은 수확 시나리오의 제어 정확도·성능·재현성.
   *조건*: Unity 실행 결과가 나와야 3종 비교가 성립. 그 전까지는 "Gazebo 실행 / Isaac·Unity 산출·정합
   검증" 이라는 **2단 표기**로 쓴다.
2. **데모 캡처** — ✅ **2026-08-14 확보**(4장): 인지+옥토맵 · Gazebo 실구동 · 실행 중 ρ 구 영역 RViz ·
   GUI 수확 패널. 같은 런에서 **수확 2 / 시도 2**, 검출 19→18→17. 조건·파일명은 `근거대장.md` 6-4.
   *남은 것*: 재계획 발화 장면(장애물 주입 시점 캡처) · 수확 전/후 동일 시점 비교컷.
3. **미해결·후속 과제** — `후속과제.md` 를 그대로 옮긴다.
4. **마일스톤 대조** — M1 달성 / M2·M3 실증 완료·**정식 판정은 사용자** / M4 진행 중.
